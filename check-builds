#!/bin/bash
VENV=/tmp/asc-venv
if [ ! -f "$VENV/bin/python3" ]; then
  python3 -m venv "$VENV"
  "$VENV/bin/pip" install PyJWT cryptography requests -q
fi
"$VENV/bin/python3" "$(dirname "$0")/check-builds.py"
